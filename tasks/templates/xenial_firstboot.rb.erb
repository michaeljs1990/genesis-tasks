#!/usr/bin/env ruby
# Script to bootstrap an xenial box with all of
# the needed software to run puppet for the first time.
# After a successful run of puppet the box is moved into
# the allocated state and this script removes itself.
